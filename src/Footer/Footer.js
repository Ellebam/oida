import React from 'react'

export default function FooterSection(){
    const element = (
        <div className ='footer-section'>
            <p> links, copyright, etc.</p>
        </div>
    );

    return element;
}